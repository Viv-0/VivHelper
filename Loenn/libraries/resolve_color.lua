
function resolveOldColor()
    for _, room in state.map.rooms do 
        for _, entity in room.entities do
            LARGE SWITCH CASE
        end
    end
    for _, layer in state.map.style do
        for _, styleground in layer.children do
            -- what's the parameter format for stylegrounds
        end
    end
end
return resolveOldColor