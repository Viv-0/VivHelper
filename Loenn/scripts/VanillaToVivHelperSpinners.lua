local entities = require("entities")
local utils = require("utils")

local script = {
    name = "vivh_Vanilla2Viv",
    displayName = "Vanilla to VivHelper Spinners",
    minimumVersion = "1.0.3",
    tooltip = "Converts Vanilla Spinners to VivHelper CustomSpinners",
    parameters = {
        
    }
}