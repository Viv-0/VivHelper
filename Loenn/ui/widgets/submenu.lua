local utils = require('utils')

local submenu = {}

function submenu.getSubmenu(elements, fieldInfo, saveChangesCallback, options)
    if not elements or #elements == 0 then return end

    
end

return submenu